mannebusk-vimrc
===============

My vim setup

## Installation
If cloned into home folder, we can run this to clear out config and symlink all
files from this repo to the config folder.
``
cd ~/.config/nvim/
rm -rf *
cp -rs ~/mannebusk-vimrc/* .
```
